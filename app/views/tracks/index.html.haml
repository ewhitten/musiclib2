- title "All Tracks"

%h1
  All Tracks
- cache ["pagination", cache_key_for(Volume)] do  
  = will_paginate @tracks

%table.volume_details.with_striping
  %thead
    %tr
      %th.index
        \#
      %th.track
        Track
      %th.artist
        Artist
      %th.vol
        Album
      %th.actions
        &nbsp;
  %tbody
    - cache cache_key_for(Track) do
      = render @tracks
    
- cache ["pagination", cache_key_for(Volume)] do  
  = will_paginate @tracks