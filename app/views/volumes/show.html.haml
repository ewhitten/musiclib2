- title "#{@volume.name} - #{@volume.artist.name}"

%h1
  = "#{@volume.name_and_location} - #{@volume.artist.name}"

%h3
  = link_to "Back to Albums", volumes_path
  
%table.volume_details.with_striping
  %thead
    %tr
      %th.index
        \#
      %th.track
        Track
      %th.artist
        Artist
      %th.vol
        Album
      %th.actions
        &nbsp;
  %tbody
    = render @volume.tracks