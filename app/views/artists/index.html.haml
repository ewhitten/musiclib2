- title "All Artists"

%table.with_striping
  %thead
    %tr
      %th.artist
        Artist
      %th.actions
        = link_to "Add an Artist", new_artist_path, remote: true
  %tbody
    - cache cache_key_for(Artist) do
      = render @artists
